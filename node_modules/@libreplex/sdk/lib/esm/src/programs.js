import { Program } from "@coral-xyz/anchor";
import { LIBREPLEX_METADATA_PROGRAM_ID, LIBREPLEX_NFT_PROGRAM_ID } from "./constants";
export async function loadMetadataProgram(provider) {
    return new Program((await import("@libreplex/idls/lib/cjs/libreplex_metadata")).IDL, LIBREPLEX_METADATA_PROGRAM_ID, provider);
}
export async function loadNftProgram(provider) {
    return new Program((await import("@libreplex/idls/lib/cjs/libreplex_nft")).IDL, LIBREPLEX_NFT_PROGRAM_ID, provider);
}
//# sourceMappingURL=programs.js.map