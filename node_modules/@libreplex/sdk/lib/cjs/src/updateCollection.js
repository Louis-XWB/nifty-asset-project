"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateCollectionAuthority = void 0;
const programs_1 = require("./programs");
function updateCollectionAuthority({ connector, collection, new_authority }) {
    return __awaiter(this, void 0, void 0, function* () {
        const metadataProgram = connector.type === "program" ? connector.metadataProgram : yield (0, programs_1.loadMetadataProgram)(connector.provider);
        const me = metadataProgram.provider.publicKey;
        if (!me) {
            throw new Error("Provider not setup. Perhaps your wallet is not connected");
        }
        return metadataProgram.methods.updateCollectionAuthority(new_authority).accounts({
            collection,
            updateAuthority: me,
        });
    });
}
exports.updateCollectionAuthority = updateCollectionAuthority;
